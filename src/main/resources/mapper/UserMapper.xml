<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demospringmybatis.dao.UserMapper">
  <select id="selectUser2" resultType="com.example.demospringmybatis.model.User" parameterType="long">
    SELECT *
      FROM USER
     WHERE ID = #{id}
  </select>
</mapper>